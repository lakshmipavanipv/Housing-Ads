# Copy this file to .env and fill in your values.
# The .env file is never committed to git.

# Local folder where Housing-Ads-Tracker.xlsx is saved.
# Windows example:
LOCAL_SAVE_PATH=C:/Projects/Housing Ad

# Set to false to disable automatic git push after each update
GIT_AUTO_PUSH=true
